# CTO Answers — my-attribution

## API Surface
- **Read endpoints**: 8 GET routes
- **Write endpoints**: 3 POST/PUT/PATCH routes
- **Delete endpoints**: 0 DELETE routes
- **Total**: 11 endpoints detected

### Key Endpoints
  - `GET /` — root
  - `GET /v1/health` — health
  - `POST /v1/sessions` — create_session
  - `GET /v1/sessions` — list_sessions
  - `POST /v1/advertisers` — create_advertiser
  - `GET /v1/advertisers` — list_advertisers
  - `POST /v1/conversions` — create_conversion
  - `GET /v1/reports/{advertiser_id}` — advertiser_report
  - ... and 3 more

## Data Model
- **Entities stored**: advertisers, conversions, health, pixel, reports, sessions, stats
- **Storage**: SQLite (dev) / PostgreSQL (production) — data isolated by account
- **Write via**: `POST /v1/sessions`, `POST /v1/advertisers`, `POST /v1/conversions`
- **Delete via**: No delete endpoints

## Authentication & Authorization
- **Auth method**: Bearer token via `Authorization` header
- **Token source**: API key provided at registration
- **Required env vars**: API key only
- **Rate limiting**: Standard per-token limits apply
- **Scopes**: All endpoints accessible with a valid token

## Content & Data Policy
- User content stored as-is; persists until deleted by user
- HTTPS in transit; at-rest encryption depends on host
- PII (email, name) stored for account management only
- GDPR: data export/deletion available via support
- Backups: daily, 30-day retention (production)

## Infrastructure
- **Runtime**: Python 3.11+ / FastAPI | **Hosting**: Railway / Docker PaaS
- **Monitoring**: `/health` endpoint | **Uptime target**: 99.5%
- **External deps**: None (self-contained)

---
*Generated by Sheept 🐑 skillify — Attribution tracking for agent advertising*
